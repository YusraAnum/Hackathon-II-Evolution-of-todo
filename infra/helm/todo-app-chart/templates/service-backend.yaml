apiVersion: v1
kind: Service
metadata:
  name: {{ include "todo-app-chart.fullname" . }}-backend-service
  labels:
    {{- include "todo-app-chart.labels" . | nindent 4 }}
spec:
  type: {{ .Values.backend.service.type }}
  ports:
    - port: {{ .Values.backend.service.port }}
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    {{- include "todo-app-chart.selectorLabels" . | nindent 4 }}
    app: todo-backend